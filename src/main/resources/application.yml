server:
  error:
    include-message: always
    include-binding-errors: always
  port: 8085

spring:
  datasource:
    password: walkerzPOSTGRESQLPassword47
    url: jdbc:postgresql://localhost:5432/hospital_management
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: testhospitalmanagement@gmail.com
    password: $managementTestPassword01

    properties:
      mail:
        smtp:
          ssl:
            port: 465
            trust: "*" 
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

